server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    # Routing côté client (React Router) : toute route inconnue
    # retombe sur index.html pour laisser React gérer la navigation.
    location / {
        try_files $uri $uri/ /index.html;
    }
}
